<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>UniversityCareer_Active_Start_Required</fullName>
    <active>true</active>
    <description>
        Validates that a start date is required when a university career is active.
    </description>
    <errorConditionFormula>
        AND(
            Active__c,
            ISBLANK(StartDate__c)
        )
    </errorConditionFormula>
    <errorMessage>
        A start date is required when a university career is active.
    </errorMessage>
</ValidationRule>